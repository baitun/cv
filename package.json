{
  "dependencies": {
    "hackmyresume": "^1.9.0-beta",
    "react": "^15.0.0",
    "react-awesome-resume": "^0.1.0",
    "react-dom": "^15.0.0",
    "react-scripts": "^2.1.1",
    "semantic-ui-css": "^2.4.1"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test --env=jsdom",
    "predeploy": "yarn build",
    "deploy": "gh-pages -d build",
    "eject": "react-scripts eject",
    "publish": "resume publish --theme kendall --resume src/resume.json",
    "serve": "resume serve --theme kendall --resume src/resume.json --dir public/resume"
  },
  "homepage": "https://savinyurii.ru/",
  "license": "MIT",
  "browserslist": [
    ">0.2%",
    "not dead",
    "not ie <= 11",
    "not op_mini all"
  ],
  "devDependencies": {
    "gh-pages": "^2.0.1"
  }
}
